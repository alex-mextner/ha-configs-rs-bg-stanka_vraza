# Ubee router connected-devices sensor (fork: alex-mextner/ha-ubee).
# Exposes sensor.ubee_devices with a .devices attribute (MAC+IP, heuristic names).
sensor:
  - platform: ubee
    host: 192.168.0.1
    username: admin
    password: !secret ubee_password
    model: EVW32C-0N
